import React, {Component} from 'react';

export default class erroralertsignup extends Component {
    render(){
        return(
        <div className="" role="alert">
            The user with this EmailId already exits
        </div>
        )
    }
}